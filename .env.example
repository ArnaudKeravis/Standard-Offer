# Shared door code for the whole CoDesign sandbox (hub, workshops, Labs, Studio).
# Optional. In production a fallback hash is used when this is unset. Set it to rotate.
SANDBOX_ACCESS_SECRET=

# Shared password for the FY27 people coverage board at /people.
# Required. If unset, the board stays locked and shows no cost data.
PEOPLE_BOARD_ACCESS_SECRET=
